---
name: Something's wrong
about: The agent is silent, an install failed, or something behaves oddly
---

**What happened, in one or two sentences**



**The doctor report** — run `scripts\doctor.cmd` and paste the whole block
(it redacts your phone number and tokens itself):

```
paste here
```

**If this is an install problem**: attach `install\install.log` too.

**If the agent is silent**: since roughly when? Did the ~20-day WhatsApp
pairing maybe expire? (`docs/RE-PAIRING.md` fixes that one in two minutes —
worth trying before filing.)
