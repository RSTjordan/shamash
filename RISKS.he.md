# לקרוא לפני שמתקינים

זה לא כתב־ויתור שגוללים מעליו. להתקנה של שמש יש השלכות אמיתיות, וחלקן
בלתי־הפיכות. המתקין יציג את הקובץ הזה ויבקש שתקליד `yes`.

## 1. הוא מריץ קוד על המחשב שלך, כתגובה להודעות

ה־watcher לוקח הודעות וואטסאפ ומזין אותן לסשן Claude Code על המכונה שלך. זו
כל המהות — וזה אומר ש**מי שמצליח להכניס הודעה לצ'אט של הסוכן יכול, בפועל,
לבקש מהמחשב שלך לעשות משהו**.

הבלמים, בדיוק כפי שהם:

- רק הודעות מהחשבון *שלך*, בצ'אט אחד או שניים שהגדרת, נחשבות פקודות.
- תוכן שהועבר, הודבק או צוטט ממישהו אחר נחשב מידע לניתוח, לא הוראות. חשוב
  להבין: זו *הוראה למודל* — היא מחזיקה בבדיקות, אבל היא לא ערובה מכנית.
  הערובה המכנית היא השורה הבאה.
- כברירת מחדל, רשימת הכלים המאושרים־מראש היא **קריאה בלבד** (`Read`,
  `Glob`, `Grep`) ועוד כלי הוואטסאפ והיומן. כל דבר אחר — כל פקודת shell, כל
  כתיבה או עריכה של קובץ — נעצר וחוזר אליך בוואטסאפ ככרטיס אישור. 👍 מאשר
  את הפעולה הזו בלבד. ❤️ מאשר אותה **וגם שומר כלל קבוע** שמאשר אותה
  אוטומטית מכאן והלאה; כל ❤️ מחליש את הבלם, בכוונה ולתמיד (הכללים נשמרים
  ב־`.claude/settings.local.json` — משם גם מוחקים).
- אם תרחיב את `allowed_tools` ב־`config.json` (למשל תוסיף `Bash` או
  `PowerShell`, כמו שהמחבר מריץ אצלו) — הכלים האלה ירוצו **בלי שום
  כרטיס**. מאותו רגע, הזרקת־פרומפט אחת מוצלחת — בתוך הודעה שהועברה, מייל
  שהסריקה קוראת, דף אינטרנט — היא הרצת קוד ללא השגחה על המחשב שלך. שים לב
  גם שהסוכן יכול לכתוב ל־`state/schedule.json`, שמריץ פקודות כל 5 דקות —
  נתיב התמדה קל לקוד מוזרק. `permission_mode: "auto"` מחליף את הכרטיסים
  במסווג הבטיחות המובנה של Claude — מהיר יותר, ועסקה שונה באמת. עשה כל
  שינוי כזה בידיעה מדויקת מה הסרת.

## 2. היסטוריית הוואטסאפ שלך יושבת בקבצים מקומיים, לא מוצפנת

הגשר מסנכרן את ההודעות שלך לבסיס נתונים SQLite על הדיסק — מי שמחזיק את
הלפטופ שלך מחזיק את ארכיון ההודעות שלך, בלי הטלפון ובלי ה־PIN. גם הלוגים
של הערכה עצמה (`logs/commands.log`, `state/actions.jsonl`,
`state/approvals.jsonl`) מכילים את טקסט הפקודות, תשובות הסוכן ופעולות
שאושרו. תוכן ההודעות גם נשלח ל־Anthropic כחלק מהפרומפטים, כמו כל דבר אחר
שאתה שם ב־Claude.

## 3. הוא שולח הודעות בשמך, ואין ביטול

הסוכן עונה לאנשים תחת השם שלך. אין להם דרך לדעת. הודעה שגויה היא הודעה
אמיתית לאדם אמיתי — הדייג'סט הוא יומן הביקורת שלך, אבל הוא מגיע *אחרי*
מעשה.

אם המשפט הזה הטריד אותך — התחל בלי תשובות אוטונומיות.

## 4. סיכון חסימה על המספר האישי שלך

זה משתמש בקליינט וואטסאפ לא־רשמי
([verygoodplugins/whatsapp-mcp](https://github.com/verygoodplugins/whatsapp-mcp),
פורק מתוחזק של lharries/whatsapp-mcp) המקושר כ"מכשיר מקושר". זה מנוגד
לתנאי השימוש של וואטסאפ. הסיכון שנצפה נמוך, אבל לא אפס — והחשבון שעל הכף
הוא האישי שלך. אף אחד לא יכול לערער על חסימה בשמך.

## 5. עלות

כל פקודה, כל סריקה וכל משמרת של עבודת־רקע מריצות סשן Claude Code — כברירת
מחדל על **Opus** (‏`"model"` ב־`config.json` הוא כפתור העלות). שתי סריקות
ביום ושיחה רגילה נכנסות במנוי Max; מנוי Pro יגיע למגבלות מהר. *עבודת רקע
פעילה* מוסיפה עד 45 דקות Opus כל 3 שעות — כמה שעות־מודל ביום כל עוד היא
רצה. הדייג'סט תמיד מראה מה רץ; `paused` לא עולה כלום.

## 6. זה עובד רק כשהמחשב דולק

- המחשב חייב להישאר ער ומחובר. אתחול שנעצר במסך הכניסה משאיר את המשימות
  המתוזמנות מתות ואת הסוכן נעלם בשקט.
- הקישור לוואטסאפ פג בערך כל 20 יום וצריך לסרוק QR מחדש
  (`docs/RE-PAIRING.md` — שתי דקות).
- אם יש מספר שני ייעודי וה־SIM או המנוי שלו פג — הערוץ הזה מת בשקט.

## 7. הסרה מוחקת פחות ממה שנדמה

`scripts\uninstall.cmd` מסיר את המשימות המתוזמנות ועוצר את התהליכים. הוא
בכוונה **לא** מוחק: את הריפו, את `config.json` ואת ה־brief שלך, את מאגרי
הגשר (כל היסטוריית ההודעות שלך, לא מוצפנת) ואת ה־outbox — הוא מדפיס איפה
כל אחד יושב. הוא גם לא יכול לנתק את המכשיר מהטלפון שלך (וואטסאפ ← מכשירים
מקושרים) או לבטל את מחברי Google שאישרת ב־claude.ai — עשה זאת ידנית אם
סיימת.

## 8. תמיכה

פרויקט אישי, משותף כי אולי יעזור למישהו. הוא **מעולם לא הותקן על מכונה
שאינה של המחבר** — המתקינים הראשונים הם טייסי ניסוי. בלי אחריות ובלי
הבטחת זמן תגובה — אבל הרץ `scripts\doctor.cmd`, פתח issue עם הפלט, ואשתדל
לעזור. PRs יתקבלו בשמחה.
